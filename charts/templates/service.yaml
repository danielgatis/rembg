apiVersion: v1
kind: Service
metadata:
  name: {{ include "rembg.fullname" . }}
  labels:
    {{- include "rembg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rembg.selectorLabels" . | nindent 4 }}
