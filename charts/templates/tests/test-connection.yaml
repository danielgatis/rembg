apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rembg.fullname" . }}-test-connection"
  labels:
    {{- include "rembg.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rembg.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
